---
name: setup
description: Triggers the full Huddle project initialization.
---
Initialize the Huddle workspace using the 'huddle-liftoff' skill. 
Ensure the framework is flattened into the root, the runtime is sibling to framework, 
and the huddle component is created with its initial entities.
